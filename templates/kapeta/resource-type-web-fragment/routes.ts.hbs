//#FILENAME:{{backendBasePath}}/.generated/proxies/fragments/{{type data.metadata.name}}-routes.ts:write-always
//
// GENERATED SOURCE - DO NOT EDIT
//
import {Router} from "express";
import { createProxyRoute, ProxyRouteOptions } from "@kapeta/sdk-proxy-route";
import {ConfigProvider} from "@kapeta/sdk-config";

export const create{{type data.metadata.name}}Router = async (provider: ConfigProvider, opts:ProxyRouteOptions = {}) => {
    const router = Router();
    router.use("{{string data.spec.path}}", await createProxyRoute(provider, "{{string data.metadata.name}}", 'web', opts));

    return router;
}