//#FILENAME:src/browser/fragments/{{type data.metadata.name}}Fragment.tsx:write-always
//
// GENERATED SOURCE - DO NOT EDIT
//

import React from "react";

let BASE_PATH = "fragments{{string data.spec.path}}";
if (!BASE_PATH.endsWith("/")) {
    //Make sure the path ends with a slash so that relative paths work
    BASE_PATH += "/";
}

export const {{type data.metadata.name}}Fragment = (props:any) => {

    return (
        <iframe src={BASE_PATH} {...props}></iframe>
    );
};
