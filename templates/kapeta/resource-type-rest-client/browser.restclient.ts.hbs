//#FILENAME:src/browser/clients/{{type data.metadata.name}}Client.ts:write-always
//
// GENERATED SOURCE - DO NOT EDIT
//

import { RestClient } from "@kapeta/sdk-web-rest-client";
{{#eachTypeReference data.spec.methods all=true}}
import { {{type name}} } from "../../entities/{{type name}}";
{{/eachTypeReference}}


export class {{type data.metadata.name}}Client {

    private client:RestClient;

    constructor() {
        this.client = new RestClient('api/rest/{{string data.metadata.name}}');
    }

{{#methods data.spec.methods}}

    /**
    * {{comment description}}
    * HTTP: {{./method}} /api/rest/{{../data.metadata.name}}{{path}}
    */
    async {{method methodName}}(
    {{#arguments arguments}}
        {{variable argumentName}}:{{fieldtype this}}
    {{/arguments}}
    ):Promise<{{returnType responseType}}{{#ifValueType responseType}}|null{{/ifValueType}}> {

    {{#ifValueType responseType}}const result = {{/ifValueType}}await this.client.execute(
    '{{string method}}',
    '{{string path}}',
    [
    {{#arguments arguments}}
        {
        name: '{{string argumentName}}',
        value: {{variable argumentName}},
        transport: '{{string transport}}'
        }
    {{/arguments}}
    ]
    );

    {{#ifValueType responseType}}
        if (result === null) {
            return null;
        }
        return result as {{returnType ../responseType}};
    {{/ifValueType}}
    }

{{/methods}}
}