//#FILENAME:src/server/.generated/proxies/rest/{{type data.metadata.name}}-routes.ts:write-always
//
// GENERATED SOURCE - DO NOT EDIT
//
import {Router} from "express";
import { createProxyRoute } from "@kapeta/sdk-proxy-route";
import {ConfigProvider} from "@kapeta/sdk-config";

export const create{{type data.metadata.name}}Router = async (provider: ConfigProvider) => {
    const router = Router();
    router.use("/api/rest/{{string data.metadata.name}}", await createProxyRoute(provider, "{{string data.metadata.name}}", 'rest'));

    return router;
}