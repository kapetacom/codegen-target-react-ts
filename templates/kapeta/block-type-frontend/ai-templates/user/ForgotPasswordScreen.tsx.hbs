#FILENAME:src/browser/screens/ForgotPasswordScreen.tsx:write-never
{{ai-type 'web-screen'}}
{{#ai-comment}}
    This is a screen component that allows users to reset their password by sending a password reset link to their email.
{{/ai-comment}}
import React, { useState, useCallback } from 'react';
import { Container, TextField, Button, Typography, Link as MuiLink, Grid } from '@mui/material';
import { Link as RouterLink } from 'react-router-dom';

{{> helpers/dsl-imports.hbs}}

export const ForgotPasswordScreen: React.FC = () => {
    const [email, setEmail] = useState('');

    {{#ai-comment}}
        Add constants for the API client hooks
    {{/ai-comment}}

    const handleSubmit: React.FormEventHandler = useCallback((event) => {
        event.preventDefault();
        {{#ai-comment}}
            Use the right API client and call the right method to handle password reset
        {{/ai-comment}}
    }, []);

    {{{{raw}}}}
    return (
        <Container
            component="main"
            maxWidth="xs"
            sx=&#123;{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'flex-start',
                justifyContent: 'center',
                height: '100vh',
                bgcolor: 'background.paper',
                padding: 2,
            }}
        >
            <Typography component="h1" variant="h5">
                Reset your password
            </Typography>

            <Typography variant="body2" sx=&#123;{ fontSize: '0.875rem', marginBottom: 2, color: 'text.disabled' }}>
                Enter your email to receive a password reset link
            </Typography>

            <form onSubmit={handleSubmit} style={{ width: '100%' }}>
                <TextField
                    variant="outlined"
                    margin="normal"
                    required
                    fullWidth
                    id="email"
                    label="Email Address"
                    name="email"
                    autoComplete="email"
                    autoFocus
                    value={email}
                    onChange={(e) => setEmail(e.target.value)}
                />

                <Button type="submit" fullWidth variant="contained" color="primary" size="large" sx=&#123;\{ mt: 2, mb: 2 }}>
                    Send Password Reset Link
                </Button>
            </form>

            <Grid container justifyContent="flex-start">
                <Grid item>
                    <MuiLink component={RouterLink} to="/login" variant="body2">
                        Cancel
                    </MuiLink>
                </Grid>
            </Grid>
        </Container>
    );
    {{{{/raw}}}}
};