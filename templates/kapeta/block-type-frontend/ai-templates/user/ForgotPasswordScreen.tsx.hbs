#FILENAME:src/browser/screens/ForgotPasswordScreen.tsx:write-never
{{ai-type 'web-screen'}}
{{#ai-comment}}
    This is a screen component that allows users to reset their password by sending a password reset link to their email.

    Suggested route: /lost-password
{{/ai-comment}}
import React, { useState, useCallback } from 'react';
import { Container, TextField, Button, Typography, Link as MuiLink, Grid } from '@mui/material';
import { Link as RouterLink } from 'react-router-dom';

{{> helpers/dsl-imports.hbs}}


export const ForgotPasswordScreen: React.FC = () => {
    const [email, setEmail] = useState('');

    /**
     * STORM-AI: Add constants for the API client hooks
     */

    const handleSubmit: React.FormEventHandler = useCallback((event) => {
        event.preventDefault();
        /**
         * STORM-AI: Use the right API client and call the right method to handle password reset
         */
    }, []);

    {{#raw}}
    return (
        <Container component="main" maxWidth="xs">
            <Typography component="h1" variant="h5">
                Forgot Password
            </Typography>

            <form onSubmit={handleSubmit}>
                <TextField
                    variant="outlined"
                    margin="normal"
                    required
                    fullWidth
                    id="email"
                    label="Email Address"
                    name="email"
                    autoComplete="email"
                    autoFocus
                    value={email}
                    onChange={(e) => setEmail(e.target.value)}
                />

                <Button type="submit" fullWidth variant="contained" color="primary">
                    Send Password Reset Link
                </Button>
            </form>

            <Grid container justifyContent="flex-end" style={{ marginTop: '10px' }}>
                <Grid item>
                    <MuiLink component={RouterLink} to="/login" variant="body2">
                        Login
                    </MuiLink>
                </Grid>
                {/**
                 * STORM-AI: If the app does not have a "Sign Up" screen, delete this Grid item
                 */}
                <Grid item>
                    <MuiLink component={RouterLink} to="/signup" variant="body2" style={{ marginLeft: '15px' }}>
                        Sign Up
                    </MuiLink>
                </Grid>
            </Grid>
        </Container>
    );
    {{/raw}}
};