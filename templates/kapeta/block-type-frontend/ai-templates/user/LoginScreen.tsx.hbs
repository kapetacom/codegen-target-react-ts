#FILENAME:src/browser/screens/LoginScreen.tsx:write-never
{{ai-type 'web-screen'}}
{{#ai-comment}}
    This is a screen component that allows users to sign in to the application.
{{/ai-comment}}
import React, { useState, useCallback } from 'react';
import { Container, TextField, Button, Typography, Link as MuiLink, Grid } from '@mui/material';
import { Link as RouterLink, useNavigate } from 'react-router-dom';

{{> helpers/dsl-imports.hbs}}

export const LoginScreen: React.FC = () => {
    const [email, setEmail] = useState('');
    const [password, setPassword] = useState('');
    {{#ai-comment}}
        Add constants for the API client hooks
    {{/ai-comment}}
    const navigate = useNavigate();

    const handleSubmit: React.FormEventHandler = useCallback((event) => {
        event.preventDefault();
        {{#ai-comment}}
            Use the right API client and call the right method that authenticates the user
            Then navigate to the main screen
        {{/ai-comment}}
    }, []);

    {{#raw}}
    return (
        <Container
            component="main"
            maxWidth="xs"
            sx=&#123;{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'flex-start',
                justifyContent: 'center',
                height: '100vh',
                bgcolor: 'background.paper',
                padding: 2,
            }}
        >
            <Typography component="h1" variant="h5">
                Log In
            </Typography>

            <Typography variant="body2" sx=&#123;{ fontSize: '0.875rem', marginBottom: 2, color: 'text.disabled' }}>
                Welcome back
            </Typography>

            <form onSubmit={handleSubmit} style={{ width: '100%' }}>
                <TextField
                    variant="outlined"
                    margin="normal"
                    required
                    fullWidth
                    id="email"
                    label="Email"
                    name="email"
                    autoComplete="email"
                    autoFocus
                    value={email}
                    onChange={(e) => setEmail(e.target.value)}
                />

                <TextField
                    variant="outlined"
                    margin="normal"
                    required
                    fullWidth
                    name="password"
                    label="Password"
                    type="password"
                    id="password"
                    autoComplete="current-password"
                    value={password}
                    onChange={(e) => setPassword(e.target.value)}
                />

                <Button type="submit" fullWidth variant="contained" color="primary" size="large" sx=&#123;\{ mt: 2, mb: 2 }}>
                    Sign In
                </Button>
            </form>

            <Grid container justifyContent="flex-end">
                {/**
                 * STORM-AI: If the app does not have a "Sign Up" screen, delete this Grid item
                 */}
                <Grid item>
                    <MuiLink component={RouterLink} to="/signup" variant="body2" sx=&#123;\{ marginRight: 2 }}>
                        Create account
                    </MuiLink>
                </Grid>
                {/**
                 * STORM-AI: If the app does not have a "Forgot password?" screen, delete this Grid item
                 */}
                <Grid item>
                    <MuiLink component={RouterLink} to="/forgot-password" variant="body2">
                        Forgot password?
                    </MuiLink>
                </Grid>
            </Grid>
        </Container>
    );
    {{/raw}}
};