//#FILENAME:{{backendBasePath}}/.generated/data/{{type data.metadata.name}}DB.ts:write-always
{{ai-type 'dao'}}
//
// GENERATED SOURCE - DO NOT EDIT
//
import Config,{ConfigProvider} from "@kapeta/sdk-config";
import { RedisDB as $RedisDB, createRedisClient as $createRedisClient, RedisClient, RedisOptions } from '@kapeta/sdk-redis';

export const create{{type data.metadata.name}}Client = (options?:RedisOptions, config?: ConfigProvider):Promise<RedisClient> => {
    return $createRedisClient(config ?? Config.getProvider(), '{{string data.metadata.name}}', options);
}

export class {{type data.metadata.name}}DB extends $RedisDB {
    constructor(options?:RedisOptions) {
        super('{{string data.metadata.name}}', options);
    }
}