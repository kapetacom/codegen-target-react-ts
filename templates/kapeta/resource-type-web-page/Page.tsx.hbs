//#FILENAME:src/browser/pages/{{snakecase data.metadata.name}}/{{type data.metadata.name}}Page.tsx:create-only
import React from 'react';

export const {{type data.metadata.name}}Page = () => {

    return (
        <div>{{data.metadata.name}}</div>
    );
};